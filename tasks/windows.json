{
  "description": "Executes an arbitrary shell command on windows.(without a puppet agent)",
  "input_method": "powershell",
  "parameters": {
    "command": {
      "description": "The command to run, including all arguments",
      "type": "String[1]"
    }
  }
}